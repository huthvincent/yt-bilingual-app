# 规格：<功能名>

> 规格只讲**做什么**和**为什么**，不讲怎么实现（实现写在 plan.md）。
> 让一个不懂代码的人也能看懂这个功能要解决什么问题。

**状态：** 草稿 / 已实现 ｜ **对应代码：** `<主要文件路径>`

---

## 目标
一两句话说清这个功能为用户解决什么问题。

## 用户故事
- 作为<某类用户>，我想<做某事>，以便<得到某价值>。
- （列 2–4 条最核心的）

## 功能需求
- **必须**：列出不可妥协的行为。
- **应当**：列出重要但可退让的行为。
- 每条都应可验证（能写成一句"给定…，当…，则…"）。

## 验收标准
- [ ] 给定<前提>，当<操作>，则<可观察的结果>。
- [ ]（覆盖正常路径 + 关键失败路径）

## 边界与非目标
明确**不做**什么，避免范围蔓延。列出已知的限制、依赖、未解决的问题。
